# Tuple Unpaccking

Tuple Unpacking ~
Tuple unpacking allows you to assign each item in a collection to a variable.

Example:

numbers = (1, 2, 3, 4)
a,b,c,d = numbers
print("a is : ",a, "\nb is : ",b, "\nc is : ", c, "\nd is : ", d)

Output:
